package com.georgeci.moneysurfer.uikit.tokens

import androidx.compose.ui.graphics.Color

/**
 * Fixed accent seed swatches shown in the appearance picker.
 * These are intentionally decoupled from the active theme so the picker
 * displays the same colours regardless of the currently selected scheme.
 */
object AccentSeeds {
    val Plum = Color(0xFF8E4585)
    val Sky = Color(0xFF4A90E2)
    val Sand = Color(0xFFD4A574)
    val Mint = Color(0xFF3DD9B0)
    val Rose = Color(0xFFE85A7A)
}
